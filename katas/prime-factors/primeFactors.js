/*
 * Escreva uma função que gera todos os fatores primos de um número inteiro positivo.
 * Números primos são números que têm apenas 2 fatores: 1 e ele mesmo
 */

function primeFactors(num) {
  // Função para gerar fatores primos
}

module.exports = primeFactors;
