/*
 * Criar uma função que verifica se uma palavra ou frase é um palíndromo
 * Um palíndromo é uma palavra que permanece igual se lida de tras pra frente
 */

function isPalindrome(str) {
  // Função para verificar se uma string é um palíndromo
}

module.exports = isPalindrome;
