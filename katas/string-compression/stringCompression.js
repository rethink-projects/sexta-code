/*
 * Implemente uma função que recebe uma string e retorna sua versão comprimida.
 * Ex: aabcccccaaa = a2b1c5a3
 */

function stringCompression(str) {
  // Função para comprimir a string
}

module.exports = stringCompression;
